#冒泡
def bubbleSort(arr):
    arr[10]
    for k in range:
        a=int(input())
        arr.append(a)
    for i in range(1,len(arr)):
        for j in range(0,len(arr)-i):
            if arr[j]>arr[j+1]:
                arr[j],arr[j+1]=arr[j+1],arr[j]
    return arr
print(arr)
